/** Placeholder controller for the Day Tracker view within the Calories area. */

import { initShell } from '../../app/shell.js';

initShell({
  title: 'Day Tracker',
  subtitle: 'This view is planned — see today at a glance.',
  active: 'calories'
});
